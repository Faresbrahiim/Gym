package com.gym.membershipservice.application.enums;

public enum SubscriptionStatus {
    ACTIVE,
    CANCELLED,
    PAUSED,
    EXPIRED,
    UPGRADED,
    DOWNGRADED,
    PAUSE_REQUESTED,
    FROZEN,
    CANCEL_REQUESTED
}